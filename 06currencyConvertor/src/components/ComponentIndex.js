// A good approach is if we have multiple components we can imoprt them into a index file 
// and export that file instead of exporting every component individually

import InputBox from "./Input";

export default InputBox